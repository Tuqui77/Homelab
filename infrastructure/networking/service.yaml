apiVersion: v1
kind: Service
metadata:
  name: homelab-gateway
  namespace: networking
spec:
  type: LoadBalancer
  selector:
    io.cilium.gateway: homelab-gateway
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: https
      port: 443
      targetPort: 443
