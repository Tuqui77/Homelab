apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
 name: grafana
 namespace: monitoring
spec:
 hostnames:
 - grafana.tomasmerino.com
 parentRefs:
 - name: homelab-gateway
   namespace: networking
 rules:
  - backendRefs:
    - name: kube-prometheus-grafana
      port: 80