apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: meriadb-config
  namespace: books
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-homelab
  resources:
    requests:
      storage: 1Gi
