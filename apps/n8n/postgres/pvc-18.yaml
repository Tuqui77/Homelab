kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgresql18-pv
  namespace: n8n
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-homelab
  resources:
    requests:
      storage: 30Gi
