apiVersion: v1
kind: Service
metadata:
  name: immich-postgres
  namespace: media
spec:
  type: ClusterIP
  selector:
    app: immich-postgres
    version: "14"
  ports:
    - port: 5432
      targetPort: 5432
