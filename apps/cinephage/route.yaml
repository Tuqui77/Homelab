apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: cinephage
  namespace: media
spec:
  hostnames:
    - cinephage.tomasmerino.com
  parentRefs:
    - name: homelab-gateway
      namespace: networking
  rules:
    - backendRefs:
        - name: cinephage
          port: 3000
