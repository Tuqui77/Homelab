apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shopmgr
  namespace: shopmgr
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-homelab
  resources:
    requests:
      storage: 10Gi
