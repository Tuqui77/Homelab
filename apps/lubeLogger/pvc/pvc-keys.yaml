apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lubelogger-keys
  namespace: lubelogger
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-homelab
  resources:
    requests:
      storage: 2Gi