apiVersion: apps/v1
kind: Deployment
metadata:
  name: lubelogger
  labels:
    app: lubelogger
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lubelogger
  template:
    metadata:
      labels:
        app: lubelogger
    spec:
      containers:
        - name: lubelogger
          image: ghcr.io/hargata/lubelogger:latest@sha256:ec072f19b7b9f90d38f23507ac5bb0c9295fbf70470e91e7f778a7ef51df7eef
          ports:
            - containerPort: 8080
          env:
            - name: LC_ALL
              value: es_AR
            - name: LANG
              value: es_AR
          volumeMounts:
            - mountPath: /App/data
              name: data
            - mountPath: /root/.aspnet/DataProtection-Keys
              name: keys
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: lubelogger-data
        - name: keys
          persistentVolumeClaim:
            claimName: lubelogger-keys
