apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grimmory-data
  namespace: books
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-homelab
  resources:
    requests:
      storage: 1Gi
