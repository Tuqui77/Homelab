apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
 name: paperless
 namespace: paperless
spec:
 hostnames:
 - paperless.tomasmerino.com
 parentRefs:
 - name: homelab-gateway
   namespace: networking
 rules:
  - backendRefs:
    - name: paperless
      port: 8000