apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbittorrent-config
  namespace: media
spec:
  storageClassName: nfs-homelab
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
